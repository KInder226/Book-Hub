# 🚀 Инструкция по развертыванию на Render.com

## 📋 Подготовка к деплою

Проект уже настроен для развертывания на Render.com!

## 🔧 Шаг 1: Подготовка репозитория GitHub

1. **Создайте репозиторий на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit: BookClub Hub"
   git branch -M main
   git remote add origin https://github.com/ваш-username/bookclubhub.git
   git push -u origin main
   ```

2. **Проверьте .gitignore:**
   - Убедитесь, что `.env`, `db.sqlite3`, `staticfiles/` и `media/` в .gitignore

## 🌐 Шаг 2: Создание сервисов на Render.com

### Вариант A: Использование render.yaml (Рекомендуется)

1. Зайдите на https://render.com
2. Создайте новый **Blueprint** (Синий принт)
3. Подключите ваш GitHub репозиторий
4. Render автоматически создаст все сервисы из `render.yaml`:
   - Web сервис (Django приложение)
   - PostgreSQL база данных
   - Redis для Celery

### Вариант B: Ручное создание

#### 1. Создайте PostgreSQL базу данных

1. В Render Dashboard нажмите **"New +"** → **"PostgreSQL"**
2. Настройки:
   - **Name**: `bookclubhub-db`
   - **Database**: `bookclubhub`
   - **User**: `bookclubhub_user`
   - **Plan**: Free
3. Нажмите **"Create Database"**
4. Скопируйте **Internal Database URL** (она понадобится позже)

#### 2. Создайте Redis

1. Нажмите **"New +"** → **"Redis"**
2. Настройки:
   - **Name**: `bookclubhub-redis`
   - **Plan**: Free
3. Нажмите **"Create Redis"**
4. Скопируйте **Internal Redis URL**

#### 3. Создайте Web Service

1. Нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий
3. Настройки:
   - **Name**: `bookclubhub`
   - **Environment**: `Python 3`
   - **Build Command**: `./build.sh`
   - **Start Command**: `gunicorn bookclubhub.wsgi:application`
   - **Plan**: Free

## ⚙️ Шаг 3: Настройка переменных окружения

В настройках Web Service добавьте следующие переменные:

### Обязательные переменные:

```
SECRET_KEY=сгенерируйте-новый-секретный-ключ
DEBUG=False
ALLOWED_HOSTS=ваш-сервис.onrender.com,*.onrender.com
USE_POSTGRES=True
DATABASE_URL=внутренний-url-от-postgresql
REDIS_URL=внутренний-url-от-redis
PYTHON_VERSION=3.11.0
```

### Дополнительные переменные (опционально):

```
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=ваш-email@gmail.com
EMAIL_HOST_PASSWORD=ваш-пароль-приложения
DEFAULT_FROM_EMAIL=BookClub Hub <noreply@bookclubhub.com>
```

### Как получить DATABASE_URL:

1. В Render Dashboard откройте вашу PostgreSQL базу данных
2. Найдите секцию **"Connections"**
3. Скопируйте **Internal Database URL** (начинается с `postgresql://`)
4. Вставьте в переменную `DATABASE_URL`

### Как получить REDIS_URL:

1. В Render Dashboard откройте ваш Redis
2. Скопируйте **Internal Redis URL** (начинается с `rediss://` или `redis://`)
3. Вставьте в переменную `REDIS_URL`

## 🔐 Шаг 4: Генерация SECRET_KEY

Выполните в терминале:
```bash
python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
```

Или используйте онлайн-генератор: https://djecrety.ir/

## 🚀 Шаг 5: Первый деплой

1. Нажмите **"Create Web Service"** или **"Deploy"**
2. Дождитесь завершения сборки
3. После успешного деплоя откройте ваш сайт

## 📝 Шаг 6: Создание суперпользователя

После первого деплоя создайте суперпользователя через Render Shell:

1. В Render Dashboard откройте ваш Web Service
2. Перейдите в **"Shell"**
3. Выполните:
   ```bash
   python manage.py createsuperuser
   ```
4. Следуйте инструкциям

## 🔄 Шаг 7: Миграции базы данных

Миграции выполняются автоматически в `build.sh`, но если нужно выполнить вручную:

1. Откройте **Shell** в Render Dashboard
2. Выполните:
   ```bash
   python manage.py migrate
   ```

## 📁 Шаг 8: Статические файлы

Статические файлы собираются автоматически в `build.sh` через `collectstatic`.

Если нужно обновить:
```bash
python manage.py collectstatic --no-input
```

## 🔧 Troubleshooting

### Проблема: Ошибка при сборке

**Решение:**
- Проверьте логи сборки в Render Dashboard
- Убедитесь, что все зависимости в `requirements.txt`
- Проверьте версию Python (должна быть 3.11.0)

### Проблема: Database connection error

**Решение:**
- Проверьте `DATABASE_URL` (должен быть Internal URL)
- Убедитесь, что `USE_POSTGRES=True`
- Проверьте, что PostgreSQL база создана

### Проблема: Static files не загружаются

**Решение:**
- Проверьте, что `STATIC_ROOT` настроен правильно
- Убедитесь, что WhiteNoise установлен
- Проверьте, что `collectstatic` выполняется в build.sh

### Проблема: Media files не сохраняются

**Решение:**
- На Render.com media файлы не сохраняются между деплоями
- Настройте S3 или другое хранилище для production
- Или используйте облачное хранилище (AWS S3, Cloudinary и т.д.)

## 🔒 Безопасность

1. **Никогда не коммитьте `.env` файл**
2. **Используйте разные SECRET_KEY для production**
3. **Установите DEBUG=False в production**
4. **Настройте ALLOWED_HOSTS правильно**

## 📊 Мониторинг

- Логи доступны в Render Dashboard → ваш сервис → **Logs**
- Метрики доступны в **Metrics**

## 🔄 Автоматический деплой

Render.com автоматически деплоит при каждом push в main ветку.

## 💡 Дополнительные настройки

### Настройка домена

1. В настройках Web Service найдите **"Custom Domains"**
2. Добавьте ваш домен
3. Настройте DNS записи согласно инструкциям

### Настройка Cron задач (Celery Beat)

Для периодических задач создайте отдельный Background Worker:
- **Start Command**: `celery -A bookclubhub beat -l info`

## ✅ Чеклист перед деплоем

- [ ] Репозиторий на GitHub создан и синхронизирован
- [ ] `.env` файл в `.gitignore`
- [ ] `DEBUG=False` в переменных окружения
- [ ] `SECRET_KEY` сгенерирован и установлен
- [ ] `ALLOWED_HOSTS` настроен правильно
- [ ] PostgreSQL база создана
- [ ] Redis создан (опционально, для Celery)
- [ ] `DATABASE_URL` установлен
- [ ] `REDIS_URL` установлен (если используется)
- [ ] Все миграции применены
- [ ] Суперпользователь создан

## 🎉 Готово!

Ваш сайт должен быть доступен по адресу: `https://ваш-сервис.onrender.com`

Удачи с деплоем! 🚀

