# 📚 BookClub Hub - Инструкция по деплою

## 🚀 Быстрый старт для деплоя на Render.com

Проект полностью готов к развертыванию!

## ✅ Что уже настроено:

- ✅ `render.yaml` - конфигурация для автоматического деплоя
- ✅ `build.sh` - скрипт сборки
- ✅ Настройки для PostgreSQL (DATABASE_URL)
- ✅ Настройки для Redis
- ✅ WhiteNoise для статических файлов
- ✅ Gunicorn для production
- ✅ Все зависимости в requirements.txt

## 📋 Шаги для деплоя:

### 1. Загрузите код на GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/bookclubhub.git
git push -u origin main
```

### 2. Создайте Blueprint на Render.com

1. Зайдите на https://render.com
2. Нажмите **"New +"** → **"Blueprint"**
3. Подключите ваш GitHub репозиторий
4. Render автоматически создаст все из `render.yaml`

### 3. Настройте переменные окружения

Добавьте в Web Service:
- `SECRET_KEY` - сгенерируйте новый ключ
- `DEBUG=False`
- `ALLOWED_HOSTS=ваш-сервис.onrender.com`

### 4. Дождитесь деплоя

Render автоматически:
- Установит зависимости
- Применит миграции
- Соберет статические файлы
- Запустит приложение

### 5. Создайте суперпользователя

В Render Shell:
```bash
python manage.py createsuperuser
```

## 📖 Подробная инструкция

См. файл **[DEPLOY.md](DEPLOY.md)** для детальных инструкций.

## 🔗 Полезные ссылки

- [Render.com Documentation](https://render.com/docs)
- [Django on Render](https://render.com/docs/deploy-django)
- [PostgreSQL on Render](https://render.com/docs/databases)

## ⚠️ Важно

1. **Не коммитьте `.env` файл** - он уже в `.gitignore`
2. **Используйте Internal URLs** для DATABASE_URL и REDIS_URL
3. **DEBUG должен быть False** в production
4. **Media файлы не сохраняются** между деплоями - используйте S3

---

Успешного деплоя! 🎉

