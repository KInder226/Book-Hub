# 📚 BookClub Hub - Структура проекта

## Описание

**BookClub Hub (Читайка)** - платформа для совместного чтения и обсуждения книг.

## Основные возможности

✅ **Аутентификация и профили пользователей**
- Расширенная модель пользователя
- Профили с аватарами, биографией, любимыми жанрами
- Автоматическая статистика (прочитано книг, количество клубов)

✅ **Книжные клубы**
- Создание и управление клубами
- Публичные и приватные клубы
- Система ролей: администратор, модератор, участник
- Приглашения по ссылке и email
- Установка текущей книги для чтения

✅ **Книги и прогресс чтения**
- Каталог книг с поиском и фильтрацией
- Отслеживание прогресса чтения (главы, страницы)
- Заметки по книгам
- Автоматический подсчет статистики

✅ **Система обсуждений**
- Форум в каждом клубе
- Типы постов: обсуждение, вопрос, цитата, заметка
- Иерархические комментарии (django-mptt)
- Лайки на посты и комментарии
- Теги постов (#вопрос, #спойлер, #цитата)
- Закрепление и закрытие постов
- Система жалоб и модерации

✅ **Уведомления**
- Уведомления о новых постах
- Уведомления о комментариях
- Уведомления о новых участниках
- Интеграция с django-notifications-hq

✅ **WebSocket (опционально)**
- Реальное время обновления постов и комментариев
- Django Channels для WebSocket поддержки

## Структура приложений

### 1. `accounts` - Пользователи и профили
- **Модели**: `User`, `UserProfile`
- **Функции**: Регистрация, вход, профили, редактирование профиля

### 2. `books` - Книги и прогресс
- **Модели**: `Genre`, `Book`, `ReadingProgress`
- **Функции**: Каталог книг, поиск, отслеживание прогресса

### 3. `clubs` - Книжные клубы
- **Модели**: `Club`, `ClubMembership`, `ClubInvitation`
- **Функции**: Создание клубов, присоединение, управление, приглашения

### 4. `discussions` - Обсуждения
- **Модели**: `PostTag`, `Post`, `Comment`, `PostReport`
- **Функции**: Форум, посты, комментарии, лайки, жалобы

### 5. `notifications_custom` - Расширения уведомлений
- Дополнительные настройки для уведомлений

## Технологический стек

- **Backend**: Django 4.2, Django REST Framework
- **База данных**: PostgreSQL
- **Очереди задач**: Celery + Redis
- **WebSocket**: Django Channels
- **Frontend**: Bootstrap 5
- **Иерархические структуры**: django-mptt
- **Уведомления**: django-notifications-hq
- **Изображения**: Pillow
- **Тестирование**: pytest, factory-boy

## Структура файлов

```
Book Club Hub/
├── accounts/                 # Приложение пользователей
│   ├── models.py            # User, UserProfile
│   ├── views.py             # Регистрация, профиль
│   ├── forms.py             # Формы регистрации и профиля
│   └── urls.py
├── books/                    # Приложение книг
│   ├── models.py            # Book, Genre, ReadingProgress
│   ├── views.py             # Список, детали, прогресс
│   ├── forms.py             # Формы книг и прогресса
│   ├── signals.py           # Сигналы для обновления статистики
│   └── urls.py
├── clubs/                    # Приложение клубов
│   ├── models.py            # Club, ClubMembership, ClubInvitation
│   ├── views.py             # CRUD клубов, присоединение
│   ├── forms.py             # Формы клубов
│   ├── signals.py           # Сигналы для уведомлений
│   └── urls.py
├── discussions/              # Приложение обсуждений
│   ├── models.py            # Post, Comment, PostTag, PostReport
│   ├── views.py             # Форум, посты, комментарии
│   ├── forms.py             # Формы постов и комментариев
│   ├── signals.py           # Сигналы для уведомлений
│   ├── consumers.py         # WebSocket consumers
│   └── routing.py           # WebSocket routing
├── notifications_custom/     # Расширения уведомлений
├── bookclubhub/             # Основная конфигурация
│   ├── settings.py          # Настройки проекта
│   ├── urls.py              # Главный URLconf
│   ├── wsgi.py              # WSGI конфигурация
│   ├── asgi.py              # ASGI конфигурация
│   └── celery_app.py        # Конфигурация Celery
├── templates/                # HTML шаблоны
│   ├── base.html            # Базовый шаблон
│   ├── accounts/            # Шаблоны аккаунтов
│   ├── books/               # Шаблоны книг
│   ├── clubs/               # Шаблоны клубов
│   └── discussions/         # Шаблоны обсуждений
├── static/                   # Статические файлы
├── media/                    # Загружаемые файлы
├── manage.py
├── requirements.txt
└── README.md
```

## Ключевые модели и их связи

```
User (Custom)
  ├── UserProfile (OneToOne)
  │   └── favorite_genres (M2M -> Genre)
  ├── ClubMembership (M2M через промежуточную модель)
  ├── ReadingProgress (ForeignKey)
  ├── Post (ForeignKey - author)
  └── Comment (ForeignKey - author)

Club
  ├── created_by (ForeignKey -> User)
  ├── current_book (ForeignKey -> Book)
  └── members (M2M через ClubMembership)

Book
  ├── genres (M2M -> Genre)
  └── reading_progresses (ForeignKey -> ReadingProgress)

Post
  ├── club (ForeignKey -> Club)
  ├── author (ForeignKey -> User)
  ├── tags (M2M -> PostTag)
  └── comments (ForeignKey -> Comment)

Comment (MPTT)
  ├── post (ForeignKey -> Post)
  ├── author (ForeignKey -> User)
  └── parent (TreeForeignKey -> Comment)
```

## Следующие шаги для развития

🔮 **Возможные улучшения:**

1. **Интеграция с внешними API**
   - Google Books API для автоматического получения информации о книгах
   - Open Library API

2. **Дополнительные функции**
   - Экспорт заметок в PDF
   - Календарь встреч книжных клубов
   - Zoom-ссылки для виртуальных обсуждений
   - Рейтинги и рецензии книг
   - Рекомендательная система

3. **Улучшения UX**
   - Расширенные фильтры и поиск
   - Мобильное приложение (React Native)
   - Темная тема
   - Персонализация интерфейса

4. **Производительность**
   - Кэширование (Redis)
   - Оптимизация запросов (select_related, prefetch_related)
   - Пагинация всех списков
   - Ленивая загрузка изображений

5. **Безопасность**
   - Rate limiting
   - CAPTCHA для регистрации
   - Двухфакторная аутентификация
   - Модерация контента

## Команды разработки

```bash
# Миграции
python manage.py makemigrations
python manage.py migrate

# Создание суперпользователя
python manage.py createsuperuser

# Запуск сервера разработки
python manage.py runserver

# Запуск Celery worker
celery -A bookclubhub worker -l info

# Запуск Celery beat
celery -A bookclubhub beat -l info

# Тесты
pytest

# Сбор статики
python manage.py collectstatic
```

## Лицензия

MIT License

