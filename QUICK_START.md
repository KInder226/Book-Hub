# 🚀 Быстрый старт (без PostgreSQL)

Если у вас проблемы с установкой `psycopg2-binary`, вы можете использовать SQLite для разработки.

## Шаг 1: Установка зависимостей БЕЗ psycopg2-binary

```bash
pip install -r requirements-dev.txt
```

Или установите зависимости вручную (без psycopg2-binary):

```bash
pip install Django djangorestframework celery redis django-channels channels-redis django-mptt django-notifications-hq pillow python-decouple django-debug-toolbar django-extensions pytest pytest-django factory-boy whitenoise
```

## Шаг 2: Создайте файл .env (опционально)

Скопируйте `.env.example` в `.env`:

```bash
copy .env.example .env
```

Или просто убедитесь, что `USE_POSTGRES=False` (это значение по умолчанию).

## Шаг 3: Примените миграции

```bash
python manage.py migrate
```

SQLite база данных будет создана автоматически в файле `db.sqlite3`.

## Шаг 4: Создайте суперпользователя

```bash
python manage.py createsuperuser
```

## Шаг 5: Запустите сервер

```bash
python manage.py runserver
```

Откройте браузер: http://127.0.0.1:8000/

---

## ⚠️ Важно

- **SQLite** подходит только для разработки и тестирования
- Для продакшена используйте **PostgreSQL**
- При переходе на PostgreSQL нужно будет:
  1. Установить `psycopg2-binary`
  2. Установить и настроить PostgreSQL
  3. Изменить `USE_POSTGRES=True` в `.env`
  4. Создать базу данных и применить миграции заново

---

## 📚 Подробная инструкция

См. файл `INSTALL_FIX.md` для подробных решений проблемы с psycopg2-binary.

