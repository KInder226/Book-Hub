# 🔧 Решение проблемы с psycopg2-binary на Windows

## Проблема
Ошибка при установке: `Command "python setup.py egg_info" failed with error code 1`

## Решения

### ✅ Решение 1: Использовать SQLite для разработки (РЕКОМЕНДУЕТСЯ)

Для разработки можно использовать SQLite вместо PostgreSQL - это не требует установки дополнительных компонентов.

1. **Установите зависимости БЕЗ psycopg2-binary:**
```bash
pip install -r requirements-dev.txt
```

2. **Убедитесь, что в `.env` установлено:**
```
USE_POSTGRES=False
```

3. **Или просто запустите проект** - по умолчанию будет использоваться SQLite.

---

### ✅ Решение 2: Исправить установку psycopg2-binary

Если вам обязательно нужен PostgreSQL:

#### Шаг 1: Обновите pip, setuptools и wheel
```bash
python -m pip install --upgrade pip setuptools wheel
```

#### Шаг 2: Установите Visual C++ Build Tools
1. Скачайте и установите [Visual Studio Build Tools](https://visualstudio.microsoft.com/downloads/#build-tools-for-visual-studio-2022)
2. При установке выберите "C++ build tools"
3. Перезапустите командную строку

#### Шаг 3: Попробуйте установить конкретную версию
```bash
pip install psycopg2-binary==2.9.9
```

Или попробуйте более новую версию:
```bash
pip install psycopg2-binary==2.9.9 --upgrade
```

#### Шаг 4: Альтернатива - установка через pre-built wheel
```bash
pip install --only-binary :all: psycopg2-binary
```

---

### ✅ Решение 3: Использовать готовый wheel файл

1. Найдите подходящий wheel файл на [PyPI](https://pypi.org/project/psycopg2-binary/#files)
2. Скачайте wheel файл для вашей версии Python и архитектуры (win_amd64)
3. Установите напрямую:
```bash
pip install путь_к_файлу.whl
```

---

### ✅ Решение 4: Использовать psycopg2 (без -binary)

**Внимание:** Это требует компилятора C++

```bash
# Удалите psycopg2-binary из requirements.txt
# Замените на:
psycopg2
```

Но для этого всё равно потребуется компилятор.

---

## Проверка установки

После установки проверьте:

```bash
python -c "import psycopg2; print(psycopg2.__version__)"
```

---

## Рекомендации

- **Для разработки**: Используйте SQLite (Решение 1) - это самый простой вариант
- **Для продакшена**: Используйте PostgreSQL с правильно установленным psycopg2-binary

---

## Настройка проекта для SQLite

Проект уже настроен для работы с SQLite по умолчанию. Просто:

1. Не устанавливайте psycopg2-binary
2. Используйте `requirements-dev.txt` или создайте `.env` с `USE_POSTGRES=False`
3. Запустите миграции:
```bash
python manage.py migrate
```

SQLite будет использоваться автоматически!

