# ✅ Чеклист для деплоя на GitHub и Render.com

## 📦 Шаг 1: Подготовка к публикации

### Проверьте .gitignore

Убедитесь, что в `.gitignore` есть:
- `.env` - переменные окружения
- `db.sqlite3` - локальная база данных
- `staticfiles/` - собранные статические файлы
- `media/` - загруженные файлы пользователей
- `__pycache__/` - кэш Python
- `*.pyc` - скомпилированные файлы

### Проверьте файлы для деплоя

Убедитесь, что созданы:
- ✅ `render.yaml` - конфигурация Render.com
- ✅ `build.sh` - скрипт сборки
- ✅ `requirements.txt` - все зависимости включены
- ✅ `DEPLOY.md` - подробная инструкция

## 📤 Шаг 2: Загрузка на GitHub

### 1. Инициализируйте Git (если еще не сделано):

```bash
git init
git add .
git commit -m "Initial commit: BookClub Hub ready for deployment"
```

### 2. Создайте репозиторий на GitHub:

1. Зайдите на https://github.com
2. Нажмите **"New repository"**
3. Название: `bookclubhub` (или ваше)
4. Выберите **Public** или **Private**
5. **НЕ** добавляйте README, .gitignore, лицензию (у нас уже есть)
6. Нажмите **"Create repository"**

### 3. Подключите удаленный репозиторий:

```bash
git branch -M main
git remote add origin https://github.com/ваш-username/bookclubhub.git
git push -u origin main
```

## 🌐 Шаг 3: Деплой на Render.com

### Вариант A: Blueprint (Рекомендуется)

1. Зайдите на https://render.com
2. Войдите/зарегистрируйтесь
3. Нажмите **"New +"** → **"Blueprint"**
4. Подключите ваш GitHub репозиторий
5. Render автоматически прочитает `render.yaml` и создаст:
   - Web Service
   - PostgreSQL база данных
   - Redis
6. Нажмите **"Apply"** и дождитесь создания

### Вариант B: Ручное создание

См. подробную инструкцию в [DEPLOY.md](DEPLOY.md)

## ⚙️ Шаг 4: Настройка переменных окружения

### В Render Dashboard откройте ваш Web Service

Добавьте переменные окружения:

#### Обязательные:

```
SECRET_KEY=сгенерируйте-новый-секретный-ключ
DEBUG=False
ALLOWED_HOSTS=ваш-сервис.onrender.com
USE_POSTGRES=True
PYTHON_VERSION=3.11.0
```

#### Автоматически добавляются из render.yaml:

- `DATABASE_URL` - автоматически из PostgreSQL
- `REDIS_URL` - автоматически из Redis

### Генерация SECRET_KEY:

```bash
python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
```

Или онлайн: https://djecrety.ir/

## 🚀 Шаг 5: Первый деплой

1. После создания сервисов нажмите **"Manual Deploy"** → **"Deploy latest commit"**
2. Дождитесь завершения сборки (может занять 5-10 минут)
3. Проверьте логи, если есть ошибки

## 👤 Шаг 6: Создание суперпользователя

1. В Render Dashboard откройте ваш Web Service
2. Перейдите в **"Shell"**
3. Выполните:
   ```bash
   python manage.py createsuperuser
   ```
4. Введите данные суперпользователя

## ✅ Шаг 7: Проверка

1. Откройте ваш сайт: `https://ваш-сервис.onrender.com`
2. Проверьте главную страницу
3. Проверьте админку: `https://ваш-сервис.onrender.com/admin/`
4. Войдите как суперпользователь

## 🔧 Troubleshooting

### Ошибка сборки

**Проверьте:**
- Все зависимости в `requirements.txt`
- Версия Python (должна быть 3.11.0)
- Логи сборки в Render Dashboard

### Ошибка базы данных

**Проверьте:**
- PostgreSQL база создана
- `DATABASE_URL` установлен правильно
- `USE_POSTGRES=True`

### Статические файлы не загружаются

**Решение:**
- WhiteNoise установлен
- `collectstatic` выполняется в build.sh
- Проверьте логи сборки

## 📝 Файлы для проверки перед коммитом

Убедитесь, что эти файлы есть в репозитории:

- ✅ `render.yaml`
- ✅ `build.sh`
- ✅ `requirements.txt`
- ✅ `.gitignore`
- ✅ `DEPLOY.md`
- ✅ `README.md` или `GITHUB_README.md`
- ✅ `.env.example` (без секретов!)

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет доступен по адресу:
**https://ваш-сервис.onrender.com**

---

**Удачи с деплоем! 🚀**

