# ⚠️ Важное замечание о версии Python

## Текущая версия: Python 3.6.0

**Ваша версия Python устарела!** Некоторые функции проекта требуют Python 3.7 или выше.

## 🔧 Что было сделано

Временно отключены компоненты, требующие Python 3.7+:
- ✅ Django Channels (WebSocket) - отключено
- ✅ ASGI приложение - отключено

**Проект будет работать**, но без WebSocket функций.

## 🚀 Рекомендация: Обновить Python

Для полной функциональности рекомендуется обновить Python до версии **3.8+**.

### Способы обновления:

1. **Через официальный сайт:**
   - Скачайте Python 3.10 или 3.11 с [python.org](https://www.python.org/downloads/)
   - При установке выберите "Add Python to PATH"
   - Переустановите зависимости в новом окружении

2. **Через pyenv (рекомендуется для разработчиков):**
   ```bash
   pyenv install 3.10.11
   pyenv global 3.10.11
   ```

3. **Через Anaconda/Miniconda:**
   ```bash
   conda create -n bookclubhub python=3.10
   conda activate bookclubhub
   ```

## ✅ Временное решение

Для работы с текущей версией Python 3.6:

1. Django Channels отключен
2. WebSocket функции недоступны
3. Все остальное работает нормально

После обновления Python:

1. Раскомментируйте 'channels' в INSTALLED_APPS
2. Раскомментируйте ASGI_APPLICATION
3. Раскомментируйте CHANNEL_LAYERS
4. Переустановите зависимости

## 📋 Требования к версиям Python

- **Минимум:** Python 3.6.1 (для базовой функциональности)
- **Рекомендуется:** Python 3.8+
- **Оптимально:** Python 3.10 или 3.11

## 🔍 Проверка версии

```bash
python --version
```

Если версия ниже 3.7, некоторые функции будут недоступны.

